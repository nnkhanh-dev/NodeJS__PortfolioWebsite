<div class="bg-white p-6 rounded-lg shadow-md">
    <div class="flex flex-wrap justify-between items-center mb-4">
        <h1 class="text-2xl font-semibold">Danh Sách Danh mục Bài viết</h1>
        <a href="/admin/category-posts/create" class="bg-blue-600 text-white px-4 py-2 rounded-md hover:bg-blue-700">Thêm Danh mục</a>
    </div>
    <div class="overflow-x-auto">
        <table id="categoryTable" class="min-w-full divide-y divide-gray-200 py-5">
            <thead class="bg-gray-100 text-gray-700">
            <tr>
                <th class="px-6 py-3 text-left text-sm font-semibold">STT</th>
                <th class="px-6 py-3 text-left text-sm font-semibold">Tên</th>
                <th class="px-6 py-3 text-left text-sm font-semibold">Slug</th>
                <th class="px-6 py-3 text-left text-sm font-semibold">Mô tả</th>
                <th class="px-6 py-3 text-right text-sm font-semibold">Hành động</th>
            </tr>
            </thead>
            <tbody class="divide-y divide-gray-200 bg-white">
            {{#each categories}}
            <tr class="hover:bg-gray-50 transition-colors">
                <td class="px-6 py-3 text-sm text-gray-700 font-medium">{{increment @index}}</td>
                <td class="px-6 py-3 text-sm text-gray-800 font-semibold">{{this.name}}</td>
                <td class="px-6 py-3 text-sm text-gray-600 font-mono">{{this.slug}}</td>
                <td class="px-6 py-3 text-sm text-gray-600">
                    {{#if this.description}}
                        {{this.description}}
                    {{else}}
                        <span class="text-gray-400 italic">Chưa có mô tả</span>
                    {{/if}}
                </td>
                <td class="px-6 py-3 text-right">
                    <a href="/admin/category-posts/edit/{{this._id}}" 
                        class="inline-block bg-yellow-400 hover:bg-yellow-500 text-white text-sm font-medium px-3 py-1.5 rounded-lg transition-colors">
                        Sửa
                    </a>
                    <form action="/admin/category-posts/delete/{{this._id}}" method="POST" class="inline-block ml-2 delete-form">
                        <button type="submit" 
                            class="bg-red-500 hover:bg-red-600 text-white text-sm font-medium px-3 py-1.5 rounded-lg transition-colors">
                            Xóa
                        </button>
                    </form>
                </td>
            </tr>
            {{/each}}
            </tbody>
        </table>
    </div>
</div>

<!-- DataTable Script -->
<link rel="stylesheet" href="https://cdn.datatables.net/1.13.4/css/jquery.dataTables.min.css">
<script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
<script src="https://cdn.datatables.net/1.13.4/js/jquery.dataTables.min.js"></script>
<script>
    $(document).ready(function() {
       $('#categoryTable').DataTable({
            responsive: true,
            language: {
                search: "🔍 Tìm kiếm:",
                lengthMenu: "Hiển thị _MENU_ dòng",
                info: "Hiển thị _START_ đến _END_ trên _TOTAL_ dòng",
                paginate: {
                    previous: "← Trước",
                    next: "Sau →"
                },
                zeroRecords: "Không tìm thấy dữ liệu"
            }
        });
    });
</script>
<script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>
<script>
    document.querySelectorAll('.delete-form').forEach(form => {
        form.addEventListener('submit', function(event) {
            event.preventDefault();

            Swal.fire({
                title: 'Bạn có chắc chắn muốn xóa?',
                text: 'Danh mục có bài viết sẽ không thể xóa!',
                icon: 'warning',
                showCancelButton: true,
                confirmButtonColor: '#3085d6',
                cancelButtonColor: '#d33',
                confirmButtonText: 'Vâng, xóa nó!',
                cancelButtonText: 'Hủy'
            }).then((result) => {
                if (result.isConfirmed) {
                    form.submit();
                }
            });
        });
    });
</script>
